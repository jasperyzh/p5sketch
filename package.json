{
  "name": "p5sketch",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node generate-sketchList.js && vite",
    "build": "node generate-sketchList.js && vite build",
    "preview": "vite preview",
    "generate-sketch-list": "node generate-sketchList.js"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "p5": "^1.9.0"
  }
}
